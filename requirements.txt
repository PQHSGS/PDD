# PDD core stack (conda env: pdd — cloned from sae_circuit)
# torch 2.4.1+cu121 ; python 3.11
torch==2.4.1
transformers==4.51.0
sae-lens==5.3.0
datasets==2.21.0
huggingface-hub==0.36.2
numpy==1.26.4
scipy==1.17.1
scikit-learn==1.6.1
pandas==2.2.3
pyarrow==25.0.0
igraph==1.0.0
leidenalg==0.12.0
umap-learn==0.5.12
plotly==5.24.1
matplotlib==3.10.0
accelerate==1.14.0
tqdm==4.68.4
pyyaml==6.0.3

# Needed only for SFT/DPO runs (install with --no-deps, pinned, to avoid
# pulling in transformers>=5 which requires torch>=2.5):
# peft
# trl
# bitsandbytes